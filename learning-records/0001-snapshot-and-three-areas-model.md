# The snapshot + three-areas model is established

Chris can use the core Git model correctly: commits are snapshots (not diffs), and changes flow working directory → staging area → repository. This sets the floor — future lessons can say "stage" and "snapshot" without re-explaining.

**Evidence**: Completed the Lesson 1 drill unprompted inside the Lesson 2 drill (commit `46a43e2 "Practive the three areas"`), including the stage-then-edit-again subtlety. Answered the related quiz questions correctly across both lessons.
